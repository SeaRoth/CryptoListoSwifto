# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CryptoListoSwifto' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CryptoListoSwifto
  
  # Redux pods
  pod 'ReSwift', '~> 4.0'
  
  
  # Reactive pods
  pod 'RxSwift', '~> 4.0'
  pod 'RxCocoa', '~> 4.0'
  pod 'RxKeyboard'
  
  # Networking pods
  pod 'SDWebImage', '~> 4.0'

  target 'CryptoListoSwiftoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CryptoListoSwiftoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
